import { useState } from 'react';import { supabase } from '../lib/supabase';
export function Orders({ orders=[], reload }){const [form,setForm]=useState({orderstatus:'new',paymentstatus:'unpaid',totalamount:0,discountamount:0,shippingfee:0});async function add(){const {error}=await supabase.from('orders').insert(form); if(error) alert(error.message); else reload();}return <><h1 className="title">Đơn hàng</h1><div className="grid2"><div className="card"><h3>Tạo đơn demo</h3><input className="input" placeholder="BranchID" onChange={e=>setForm({...form,branchid:e.target.value})}/><br/><br/><input className="input" placeholder="ChannelID" onChange={e=>setForm({...form,channelid:e.target.value})}/><br/><br/><input className="input" type="number" placeholder="Tổng tiền" onChange={e=>setForm({...form,totalamount:Number(e.target.value)})}/><br/><br/><button className="btn" onClick={add}>Tạo đơn</button></div><div className="card"><h3>Danh sách đơn</h3><table><thead><tr><th>Ngày</th><th>Trạng thái</th><th>Thanh toán</th><th>Tổng</th></tr></thead><tbody>{orders.map((o,i)=><tr key={o.orderid||o.order_id||i}><td>{String(o.orderdate||o.order_date||'').slice(0,10)}</td><td>{o.orderstatus||o.order_status}</td><td>{o.paymentstatus||o.payment_status}</td><td>{Number(o.totalamount||o.total_amount||0).toLocaleString('vi-VN')}</td></tr>)}</tbody></table></div></div></>}
